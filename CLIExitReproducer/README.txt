run-cli.sh is simple script that runs CLI script in a loop 1000 times. 
By default no logging is enabled. To enable logging, provide the number (n) of the logging<n>.properties file you want to use. logging1.properties can be used.

